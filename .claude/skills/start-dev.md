---
name: start-dev
description: Start the Expo development server with cache cleared
---

Start the Expo dev server:

```bash
npx expo start --clear
```

After running, the terminal will show a QR code. Options:
- Press `i` → iOS Simulator
- Press `a` → Android Emulator
- Scan QR code with Expo Go on a real device
